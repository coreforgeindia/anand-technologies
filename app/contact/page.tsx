"use client";
import { useSite } from "@/src/hooks/useSite";

export default function Contact(){
  const { enquiry, removeFromEnquiry } = useSite();
  return <main className="container py-12"><h1 className="text-4xl font-bold">Contact and Enquiry</h1><p className="mt-3 text-[var(--muted)]">Add products to enquiry then submit requirements to our sales team.</p><div className="mt-4 rounded-xl border border-[var(--line)] bg-white p-4"><h2 className="font-semibold">Selected Products</h2>{enquiry.length===0?<p className="text-sm text-[var(--muted)]">No products selected yet</p>:<ul className="mt-2 space-y-2">{enquiry.map((item)=><li key={item} className="flex items-center justify-between rounded bg-[var(--soft)] p-2"><span>{item}</span><button onClick={()=>removeFromEnquiry(item)} className="text-sm text-red-600">Remove</button></li>)}</ul>}</div><form className="mt-8 grid gap-4 rounded-xl border border-[var(--line)] bg-white p-6 md:grid-cols-2"><input className="rounded border border-[var(--line)] p-3" placeholder="Full name"/><input className="rounded border border-[var(--line)] p-3" placeholder="Email"/><input className="rounded border border-[var(--line)] p-3" placeholder="Phone"/><input className="rounded border border-[var(--line)] p-3" placeholder="Company"/><input readOnly value={enquiry.join(" | ")} className="rounded border border-[var(--line)] p-3 md:col-span-2" placeholder="Selected products"/><textarea className="rounded border border-[var(--line)] p-3 md:col-span-2" rows={5} placeholder="Requirement details frequency bands quantity delivery timeline"/><button className="rounded bg-[var(--brand)] px-6 py-3 font-semibold text-white md:col-span-2">Submit Enquiry</button></form></main>
}
